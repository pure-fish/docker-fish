---
description: Describe when these instructions should be loaded by the agent based on task context
# applyTo: 'Describe when these instructions should be loaded by the agent based on task context' # when provided, instructions will automatically be added to the request context when the pattern matches an attached file
---

<!-- Tip: Use /create-instructions in chat to generate content with agent assistance -->

This project is a Fish shell repository. Work in English and keep responses concise; follow Fish shell syntax, prefer simple KISS solutions, avoid external dependencies where possible, and use `just` to test or build the documentation. Use Conventional Commits format (`feat:`, `fix:`, `chore:`, etc.) for commit messages.